{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "github": "nuxt build --preset github_pages"
  },
  "dependencies": {
    "@nuxtjs/sitemap": "^6.0.0-beta.2",
    "nuxt": "^3.12.4",
    "vue": "latest"
  }
}
